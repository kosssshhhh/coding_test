#include <bits/stdc++.h>
using namespace std;

string s;
int main() {
  // string buffer_stream;
  // getline(cin, buffer_stream);
  while (getline(cin, s)) {
    cout << s << '\n';
  }

  return 0;
}
